Index: src/components/auth/LoginForm.tsx
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>'use client';\r\n\r\nimport React, { useState } from 'react';\r\nimport { AuthViewMode } from './AuthModal';\r\nimport SocialLogin from './SocialLogin';\r\n\r\ninterface LoginFormProps {\r\n    setViewMode: (mode: AuthViewMode) => void;\r\n    onClose: () => void;\r\n}\r\n\r\nexport default function LoginForm({ setViewMode, onClose }: LoginFormProps) {\r\n    const [email, setEmail] = useState('');\r\n    const [password, setPassword] = useState('');\r\n    const [error, setError] = useState('');\r\n\r\n    const handleLoginSubmit = (e: React.FormEvent) => {\r\n        e.preventDefault();\r\n        if (!email || !password) {\r\n            setError('이메일과 비밀번호를 모두 입력해주세요.');\r\n            return;\r\n        }\r\n        console.log('로그인 시도 데이터:', { email, password });\r\n        alert('로그인 성공! 메인화면(F-MAP-001)으로 이동합니다.');\r\n        onClose();\r\n    };\r\n\r\n    return (\r\n        <div>\r\n            <div className=\"text-center mb-6\">\r\n                <h1 className=\"text-2xl font-bold text-gray-900\">로고</h1>\r\n                <p className=\"text-xs text-gray-400 mt-1\">소개 텍스트를 입력하는 공간입니다.</p>\r\n            </div>\r\n\r\n            <form onSubmit={handleLoginSubmit} className=\"space-y-4\">\r\n                <div>\r\n                    <input\r\n                        type=\"text\"\r\n                        placeholder=\"이메일 또는 아이디\"\r\n                        value={email}\r\n                        onChange={(e) => setEmail(e.target.value)}\r\n                        className=\"w-full px-4 py-2.5 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm\"\r\n                    />\r\n                </div>\r\n                <div>\r\n                    <input\r\n                        type=\"password\"\r\n                        placeholder=\"비밀번호\"\r\n                        value={password}\r\n                        onChange={(e) => setPassword(e.target.value)}\r\n                        className=\"w-full px-4 py-2.5 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm\"\r\n                    />\r\n                </div>\r\n\r\n                {error && <p className=\"text-xs text-red-500 font-medium\">⚠\uFE0F {error}</p>}\r\n\r\n                <button\r\n                    type=\"submit\"\r\n                    className=\"w-full py-3 bg-gray-900 hover:bg-gray-800 text-white font-medium rounded-xl text-sm transition-all\"\r\n                >\r\n                    로그인하기\r\n                </button>\r\n            </form>\r\n\r\n            {/* 공통 소셜 로그인 배치 */}\r\n            <SocialLogin />\r\n\r\n            {/* 하단 스위칭 링크 영역 */}\r\n            <div className=\"flex justify-center items-center space-x-3 text-xs text-gray-400 mt-6\">\r\n                <button onClick={() => setViewMode('FIND_ID')} className=\"hover:underline hover:text-gray-600\">아이디 찾기</button>\r\n                <span>|</span>\r\n                <button onClick={() => setViewMode('FIND_PW')} className=\"hover:underline hover:text-gray-600\">비밀번호 찾기</button>\r\n                <span>|</span>\r\n                <button onClick={() => setViewMode('SIGNUP')} className=\"hover:underline text-gray-900 font-bold\">회원가입</button>\r\n            </div>\r\n        </div>\r\n    );\r\n}
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/src/components/auth/LoginForm.tsx b/src/components/auth/LoginForm.tsx
--- a/src/components/auth/LoginForm.tsx	(revision 22a473dc4d2b81e93f65228dc9908d4814f50f0f)
+++ b/src/components/auth/LoginForm.tsx	(date 1780368350928)
@@ -1,78 +1,157 @@
 'use client';
 
 import React, { useState } from 'react';
-import { AuthViewMode } from './AuthModal';
-import SocialLogin from './SocialLogin';
 
+// 부모 컴포넌트인 AuthModal.tsx 등에 이미 선언되어 있는
+// 'SIGNUP' | 'FIND_ID' | 'FIND_PW' 모드 문자열을 매개변수 타입으로 바로 활용합니다.
 interface LoginFormProps {
-    setViewMode: (mode: AuthViewMode) => void;
+    setViewMode: (mode: 'LOGIN' | 'SIGNUP' | 'FIND_ID' | 'FIND_PW') => void;
     onClose: () => void;
 }
 
 export default function LoginForm({ setViewMode, onClose }: LoginFormProps) {
+    // 입력 필드 및 로딩 상태 관리
     const [email, setEmail] = useState('');
     const [password, setPassword] = useState('');
-    const [error, setError] = useState('');
+    const [isLoading, setIsLoading] = useState(false);
 
-    const handleLoginSubmit = (e: React.FormEvent) => {
+    // 실제 백엔드 검증 실패 메시지 출력을 위한 에러 상태
+    const [error, setError] = useState<string | null>(null);
+
+    const handleSubmit = async (e: React.FormEvent) => {
         e.preventDefault();
-        if (!email || !password) {
-            setError('이메일과 비밀번호를 모두 입력해주세요.');
+        setError(null);
+
+        if (!email.trim() || !password.trim()) {
+            setError('이메일과 비밀번호를 모두 입력해 주세요.');
             return;
         }
-        console.log('로그인 시도 데이터:', { email, password });
-        alert('로그인 성공! 메인화면(F-MAP-001)으로 이동합니다.');
-        onClose();
+
+        setIsLoading(true);
+
+        try {
+            // 1. 순수 fetch API 방식을 활용한 실제 백엔드 서버 값 검증 요청
+            const response = await fetch('http://localhost:8080/api/v1/auth/login', {
+                method: 'POST',
+                headers: {
+                    'Content-Type': 'application/json',
+                },
+                body: JSON.stringify({
+                    email: email,
+                    password: password,
+                }),
+            });
+
+            // 2. 검증 실패(401, 400 등) 시 빨간색 경고 배너 텍스트 주입
+            if (!response.ok) {
+                setError('이메일 또는 비밀번호가 다릅니다.');
+                setIsLoading(false);
+                return;
+            }
+
+            // 로그인 성공 시 응답 데이터(JWT 토큰 팩) 파싱 및 클라이언트 세션 안착
+            const data = await response.json();
+            if (data.accessToken) {
+                localStorage.setItem('user_token', data.accessToken);
+                localStorage.setItem('user_nickname', data.user?.nickname || '위치삼');
+                localStorage.setItem('user_avatar', data.user?.avatar || '🥑');
+            }
+
+            // 성공 완료 후 부모의 모달 종료 제어 함수 가동
+            onClose();
+
+        } catch (err) {
+            setError('서버 연결에 실패했습니다. 네트워크 상태를 확인해 주세요.');
+        } finally {
+            setIsLoading(false);
+        }
     };
 
     return (
-        <div>
-            <div className="text-center mb-6">
-                <h1 className="text-2xl font-bold text-gray-900">로고</h1>
-                <p className="text-xs text-gray-400 mt-1">소개 텍스트를 입력하는 공간입니다.</p>
-            </div>
+        <form onSubmit={handleSubmit} className="space-y-4 w-full text-xs select-none">
 
-            <form onSubmit={handleLoginSubmit} className="space-y-4">
-                <div>
-                    <input
-                        type="text"
-                        placeholder="이메일 또는 아이디"
-                        value={email}
-                        onChange={(e) => setEmail(e.target.value)}
-                        className="w-full px-4 py-2.5 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm"
-                    />
-                </div>
-                <div>
-                    <input
-                        type="password"
-                        placeholder="비밀번호"
-                        value={password}
-                        onChange={(e) => setPassword(e.target.value)}
-                        className="w-full px-4 py-2.5 border border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm"
-                    />
+            {/* 이메일 주소 입력 그룹 */}
+            <div className="space-y-1">
+                <label className="text-[11px] font-bold text-gray-400 block">이메일 주소</label>
+                <input
+                    type="email"
+                    value={email}
+                    onChange={(e) => setEmail(e.target.value)}
+                    placeholder="example@domain.com"
+                    disabled={isLoading}
+                    className="w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-600/20 focus:border-green-600 transition-all font-medium text-gray-800"
+                />
+            </div>
+
+            {/* 비밀번호 입력 그룹 */}
+            <div className="space-y-1">
+                <label className="text-[11px] font-bold text-gray-400 block">비밀번호</label>
+                <input
+                    type="password"
+                    value={password}
+                    onChange={(e) => setPassword(e.target.value)}
+                    placeholder="••••••••"
+                    disabled={isLoading}
+                    className="w-full px-4 py-3 bg-gray-50 border border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-600/20 focus:border-green-600 transition-all text-gray-800"
+                />
+            </div>
+
+            {/* setError 발동 시 출력되는 경고 피드백 박스 */}
+            {error && (
+                <div className="p-3 bg-red-50 border border-red-100 rounded-xl animate-in fade-in duration-150">
+                    <p className="text-[11px] font-semibold text-red-600 flex items-center">
+                        <span className="mr-1.5">⚠️</span> {error}
+                    </p>
                 </div>
+            )}
 
-                {error && <p className="text-xs text-red-500 font-medium">⚠️ {error}</p>}
-
+            {/* 로그인 실행 버튼 */}
+            <div className="pt-2">
                 <button
                     type="submit"
-                    className="w-full py-3 bg-gray-900 hover:bg-gray-800 text-white font-medium rounded-xl text-sm transition-all"
+                    disabled={isLoading}
+                    className="w-full py-3.5 bg-green-700 hover:bg-green-800 text-white font-bold rounded-xl transition-all shadow-md active:scale-[0.99] disabled:opacity-50"
                 >
-                    로그인하기
+                    {isLoading ? '인증 정보 검증 중...' : '비건 안심 지도 로그인'}
                 </button>
-            </form>
-
-            {/* 공통 소셜 로그인 배치 */}
-            <SocialLogin />
-
-            {/* 하단 스위칭 링크 영역 */}
-            <div className="flex justify-center items-center space-x-3 text-xs text-gray-400 mt-6">
-                <button onClick={() => setViewMode('FIND_ID')} className="hover:underline hover:text-gray-600">아이디 찾기</button>
-                <span>|</span>
-                <button onClick={() => setViewMode('FIND_PW')} className="hover:underline hover:text-gray-600">비밀번호 찾기</button>
-                <span>|</span>
-                <button onClick={() => setViewMode('SIGNUP')} className="hover:underline text-gray-900 font-bold">회원가입</button>
             </div>
-        </div>
+
+            {/* 하단 AuthModal 연동 3대 화면 스위칭 링크 메뉴 구역 */}
+            <div className="flex items-center justify-center space-x-3 pt-3 text-gray-400 font-medium">
+
+                {/* 1. 아이디 찾기 */}
+                <button
+                    type="button"
+                    onClick={() => setViewMode('FIND_ID')}
+                    className="hover:text-gray-600 transition-colors bg-transparent border-none p-0 cursor-pointer"
+                >
+                    아이디 찾기
+                </button>
+
+                <span className="text-gray-200">|</span>
+
+                {/* 2. 비밀번호 찾기 */}
+                <button
+                    type="button"
+                    onClick={() => setViewMode('FIND_PW')}
+                    className="hover:text-gray-600 transition-colors bg-transparent border-none p-0 cursor-pointer"
+                >
+                    비밀번호 찾기
+                </button>
+
+                <span className="text-gray-200">|</span>
+
+                {/* 3. 회원가입 */}
+                <button
+                    type="button"
+                    onClick={() => setViewMode('SIGNUP')}
+                    className="text-green-700 font-bold hover:underline bg-transparent border-none p-0 cursor-pointer"
+                >
+                    회원가입
+                </button>
+
+            </div>
+
+        </form>
     );
 }
\ No newline at end of file
